import { Component } from "@angular/core";

@Component({
  selector: 'dxn-register',
  template: `
    <p>
      auth works!
    </p>
    <a routerLink="/auth/register">Login</a>
  `,
})

export class RegisterComponent { }
